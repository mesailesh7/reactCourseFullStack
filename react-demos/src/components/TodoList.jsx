import React, { useState } from "react";

const TodoList = () => {
  const [todo, setTodo] = useState([]);

  return (
    <div>
      <input type="text" />
      <button></button>
    </div>
  );
};

export default TodoList;
