
const JSXRules = () => {
    return (
        <div>
            <h1>JSX Rules</h1>

                <ul>
                    <ol>JSX Must return a single parent element</ol>
                    <ol>JSX elements must be properly closed</ol>
                    <ol>JSX attributes are written using camelCase (e.g., className instead of a class)</ol>
                </ul>

        </div>
    )
}
export default JSXRules
